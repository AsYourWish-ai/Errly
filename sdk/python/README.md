# errly — Python SDK

Lightweight error monitoring SDK for Python. Zero external dependencies for the core package.

## Install

```bash
pip install errly
```

See the main [Errly README](https://github.com/AsYourWish-ai/Errly) for full usage docs.
